import { EqualValuesDirective } from './equal-values.directive';

describe('EqualValuesDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualValuesDirective();
    expect(directive).toBeTruthy();
  });
});
